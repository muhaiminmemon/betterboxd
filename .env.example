# Postgres connection string (Supabase, Neon, Railway, or local docker-compose)
# Local: postgres://betterboxd:betterboxd@localhost:5432/betterboxd
DATABASE_URL=

# TMDB API key (v3) — https://www.themoviedb.org/settings/api
TMDB_API_KEY=
