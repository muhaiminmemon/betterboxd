services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: betterboxd
      POSTGRES_PASSWORD: betterboxd
      POSTGRES_DB: betterboxd
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
